import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  layoutContainer: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    marginTop: '24px'
  }
}));
